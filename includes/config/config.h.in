#ifndef CONFIG_H
#define CONFIG_H

#define GIT_HASH "${GIT_COMMIT_HASH}"
#define VERSION  "${ISOCHRONE_VERSION_MAJOR}.${ISOCHRONE_VERSION_MINOR}"

extern const char* GET_COMMIT_HASH();
extern const char* GET_VERSION();

#endif